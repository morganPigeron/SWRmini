import { DebugMainAnimation } from "./mainAnimation";

describe("Main animation debug test suite", () => {
  it("need to find a way to test dom", ()=>{});
/*
  const anim = new DebugMainAnimation();

  it("exist", () => {
    expect(anim).toBeDefined();
  });
*/
});